export type CustomColorType = {
	colors: {
		background: string;
		border: string;
		bottomIconDefault: string;
		card: string;
		inputBackground: string;
		notification: string;
		primary: string;
		subText: string;
		text: string;
	};
	dark: boolean;
};